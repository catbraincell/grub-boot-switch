#
# A grub.cfg with no bootable entries at all -- what a fresh install looks like
# before update-grub has ever run. Input for:
#
#     sudo grub-boot-switch-manager tests/fixtures/empty.cfg
#
# Every slot should offer only N/A, and no automatic Rescan should fire.
#
