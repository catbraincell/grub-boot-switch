#
# A grub.cfg with no bootable entries at all. Every slot should offer only N/A,
# and the app should sit there and say so -- it is input for:
# 
#     sudo grub-boot-switch-manager tests/fixtures/empty.cfg
#
